## 문제 (서버)

1. 서버내 정보 통신
2. 웹에서 사용자의 입력에 의해 파이썬에서 모델돌리기
3. 웹으로 접속해야 하는데, 다른 컴퓨터로 들어갈 수 없다.
    - 서버 컴퓨터의 ip address로 들어가려 했는데, 아래와 같이 연결할 수 없음.

    ![https://s3-us-west-2.amazonaws.com/secure.notion-static.com/f8605b4a-2fa9-4f25-9f0b-be2853be1b7b/Untitled.png](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/f8605b4a-2fa9-4f25-9f0b-be2853be1b7b/Untitled.png)

## 해야할 것

1. 서버에서 gan 돌리기
    - 서버에서 GAN 돌리는 것이 안된다면 분류를 날린다

마스크 쓴 표정을 확인하기가 어렵다 → 그래서 마스크 안 쓴 사진을 사용한다.

사용 방법 : 

마스크 안 쓴 사진(no_mask_1.jpg)에 마스크를 씌운 사진(mask_1.jpg)을 추가한다.

마스크 안 쓴 사진의 표정을 분류한다. 

(no_mask_1.jpg)의 표정이 화난 사진이면 (mask_1.jpg)를 모델에 labeling 시킨다.

**2. 문제점**

- 눈으로 표정 확인 → x

**3. 사용자 시나리오**

1. 마스크 쓰지 않은 얼굴을 upload 한다.
2. 이 얼굴을 분류기에서 화난 표정, 무표정, 웃는 표정으로 분류해준다.
3. 자신이 마스크를 쓴 얼굴을 업로드한다.
4. 마스크 부분에 대체하고 싶은 표정을 선택한다.
5. 자신이 선택한 표정을 이용해서 gan 모델을 생성한다. 
6. 그 표정이 학습된 gan으로 표정을 생성해서 합성시킨다.
7. 끝

5월 30일 까지 끝내야 한다.

14일 남았다. 

(5월 17일 ~ 5월 30일까지)
해야할 것 : 

1. GAN 모델 train 데이터 1000장으로 설정하고 걸린 시간 기록하기 (2일 동우) GAN 학습시킨 모델 올리기 (동우) - 40장당 1시간 장당 1.5분
2. ~~얼굴 표정 분류 웹에서 제대로 동작시키기 전처리 작업(5월 18일까지 도희, 민진)~~
    - ~~완료. but, 정확도 문제가 있음. 무표정 사진 7장을 넣고 돌렸는데, angry 4, happy 3이 나옴.  모델과 다른 결과인지 확인하고~~
        - ~~만약에 모델과 같은 결과이면 모델의 Train data를 추가해서 재학습~~
        - ~~모델과 다른 결과이면 이유 파악 필요~~
3. ~~감정 분류기 parameter 바꿔서 돌리고 기록(도희)~~
4. 서버  (5일 민진)
    - ~~wsgi이용 Apache-Flask 연동 작업~~
    - 모델 돌리는 방법
        - AWS EC2 이용
        - 학교에 문의하여 서버 받아서 이용
    - 웹 서버 배포
        - 포트포워딩 - 번거로울듯
        - **ngrok** but, URL이 8시간이 최대 → 유료 서비스 사용할 경우 해결된다.
        - AWS와 같은 것들 이용
5. 모델 생성 버튼
    - ~~버튼 생성~~
    - 감정 분류 수행 이전에 **파일 개수가 기존에 설정한 조건 이상이면 GAN Train set 학습해서 학습된 표정을 나타내도록 한다.**
    - GAN 수행 이전에 **학습된 표정 나타내서 GAN 가능하도록, 학습된 표정이 아니면 GAN 불가**
6. ~~프런트 엔드 템플릿 적용시키기 부트스트랩 (꾸미기) (도희)~~
7. GAN 모델 지표 설정 (동우) (보고서 작성을 위함 2일)

```bash
pip install -r requirement.txt
```

requirement.txt 내용

```python
pip install tensorflow
pip install flask
```

5월 31일 ~ 6월 4일

1. 최종 보고서 작성

시간이 만약 남는다면?

1. mask object detection
