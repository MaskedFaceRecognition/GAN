시나리오

1. 사진 업로드 하기를 누르면 사진이 업로드가 되어 분류 된다.
2. 여기서 화난 얼굴, 웃는 얼굴, 무표정 얼굴을 분류한다.

여기서 분류된 사진이 300장 이상이 된다면 그 사진의 gan 모델+mask detecting 을 만들 수 있다.

표정별로 버튼을 설정해 어느 표정의 모델을 생성할 것인지 선택하고 모델 생성을 누르면 설정한 경로에 있는 파일을 npy파일로 만든 다음에 그 파일로 gan학습을 시킨다. 학습 epoch가 500 이상일 때,  학습 완료시키고 사진을 보내준다.
